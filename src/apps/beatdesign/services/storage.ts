import { createStorage } from '../../../shared/services/storage'

// Create BeatDesign-specific storage
export const storage = createStorage('beatdesign')

// Export the storage interface for BeatDesign
export default storage